$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json

name: en2fr-train
display_name: EN→FR training job
experiment_name: exp-en2fr
type: command

code: .
command: >
  python en2fr_translation_job.py
  --epochs 200
  --batch-size 256
  --lr 1e-3

# Use the curated environment from the public 'azureml' registry
environment: azureml://registries/azureml/environments/acpt-pytorch-2.2-cuda12.1/versions/47

compute: azureml:aml-gpu-cluster
limits:
  timeout: 86400
